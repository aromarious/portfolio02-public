import { afterAll, afterEach, beforeAll, beforeEach } from 'vitest'

beforeAll(async () => {
  // 単体テスト用のグローバルセットアップ
})

afterAll(async () => {
  // 単体テスト用のグローバルクリーンアップ
})

beforeEach(async () => {
  // 各テスト前のセットアップ
})

afterEach(async () => {
  // 各テスト後のクリーンアップ
})
